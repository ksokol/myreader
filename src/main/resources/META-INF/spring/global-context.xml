<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans" 
    xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
    xmlns:context="http://www.springframework.org/schema/context"
    xmlns:task="http://www.springframework.org/schema/task"
    xsi:schemaLocation="
        http://www.springframework.org/schema/beans 
        http://www.springframework.org/schema/beans/spring-beans-3.2.xsd
		http://www.springframework.org/schema/task
        http://www.springframework.org/schema/task/spring-task-3.2.xsd
		http://www.springframework.org/schema/context
        http://www.springframework.org/schema/context/spring-context-3.2.xsd">

    <context:annotation-config />

    <context:component-scan base-package="myreader.config, myreader.service, myreader.fetcher" />
    <task:scheduler id="myScheduler" pool-size="10" />
    <context:property-placeholder location="classpath:webapp.properties" />

    <task:scheduled-tasks scheduler="myScheduler">
        <task:scheduled ref="feedListFetcher" method="run" fixed-rate="300000"/>
        <task:scheduled ref="syndFetcher-1" method="run" fixed-rate="300000"/>
        <task:scheduled ref="syndFetcher-2" method="run" fixed-rate="300000"/>
        <!-- TODO deactivated until this job has a unittest -->
        <!-- <task:scheduled ref="purgerJob" method="run" cron="0 0 3 * * *"/> -->
        <task:scheduled ref="iconUpdateJob" method="run" cron="0 0 2 * * SUN"/>
    </task:scheduled-tasks>

    <bean id="httpConnector" class="myreader.fetcher.impl.HttpConnector">
        <property name="userAgent" value="${myr.http.useragent}" />
        <property name="connectTimeout" value="${myr.http.connectTimeout}" />
        <property name="readTimeout" value="${myr.http.readTimeout}" />
    </bean>
</beans>